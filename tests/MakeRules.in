$(TARGET):
	@SRCDIR=$(SRCDIR) PSIDATADIR=$(SRC_PSIDATADIR) ../../bin/psi4 $(SRCDIR)/input.dat | tee $(TARGET)

clean:
	@SRCDIR=$(SRCDIR) PSIDATADIR=$(SRC_PSIDATADIR) 
	/bin/rm -f $(TARGET) $(GARBAGE)

