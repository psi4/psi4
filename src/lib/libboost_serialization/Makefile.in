
srcdir = @srcdir@
VPATH = @srcdir@

include ../MakeVars

PSITARGET = $(top_objdir)/lib/libboost_serialization.a

TRUECXXSRC = $(notdir $(wildcard $(srcdir)/*.cc))

LIBOBJ = $(TRUECXXSRC:%.cc=%.o)

include ../MakeRules

install_inc:: $(DEPENDINCLUDE)
	$(MKDIRS) $(DESTDIR)$(incdir)/libboost_serialization
	$(INSTALL_INCLUDE) $^ $(DESTDIR)$(incdir)/libboost_serialization

ifneq ($(DODEPEND),no)
include $(LIBOBJ:%.o=%.d)
endif
