
srcdir = @srcdir@
VPATH = @srcdir@

include ../MakeVars

PSITARGET = $(top_objdir)/lib/libPSI_sapt.a

TRUECXXSRC = 

DEPENDINCLUDE = 

LIBOBJ = $(TRUECXXSRC:%.cc=%.o)

include ../MakeRules

install_inc:: $(DEPENDINCLUDE)
	$(MKDIRS) $(incdir)/libsapt
	$(INSTALL_INCLUDE) $^ $(incdir)/libsapt

ifneq ($(DODEPEND),no)
include $(LIBOBJ:%.o=%.d)
endif

