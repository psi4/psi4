if(TARGET PCMSolver::PCMSolver)
    set(sources_list psipcm.cc host_writer.cpp)
    psi4_add_module(lib psipcm sources_list mints)
    target_link_libraries(psipcm PRIVATE PCMSolver::PCMSolver)
endif()
