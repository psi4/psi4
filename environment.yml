name: p4env311
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.11
  - psi4
  - conda-forge/label/libint_dev::libint
  - dftd3-python<1.3 # 1.3.0 and beyond requires changes from past the 1.10.0 release and QCEngine >=0.50.0
